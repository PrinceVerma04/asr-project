name: asr_mobile_app
description: Mobile client for the Cohere ASR backend.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^8.1.2
  http: ^1.2.2

flutter:
  uses-material-design: true
